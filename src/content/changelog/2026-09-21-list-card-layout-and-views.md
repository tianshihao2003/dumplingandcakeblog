---
version: "v1.39.0"
date: 2026-09-21
time: "17:50"
type: feature
description: 文章列表卡片改为两行固定布局（分类单独一行、日期字数另起一行），并新增每篇文章浏览量显示
---

## 列表卡片两行布局 + 浏览量

- 文章列表页与分类页的卡片（网格视图与列表视图），元数据改为**固定两行**：第一行是分类（保留原有分类色）与浏览量，第二行是发布日期与字数（列表视图另有标签）。此前这些信息挤在一行里自动折行，分类名一长就多占一行，同一屏里卡片高度参差不齐；现在每张卡片结构一致，高度对齐。
- 摘要统一收成 **2 行**（网格卡片、以及列表卡片在移动端的显示），卡片更紧凑，同屏能看到更多篇。
- 列表视图的浏览量紧随分类之后显示，网格视图仍放在该行最右侧。
- 新增**每篇文章的浏览量**：数字来自 Waline 计数，页面打开时把当前这一页所有文章的路径一次性批量查询，不在列表页产生额外浏览计数。评论服务未配置或请求失败时，眼睛图标与数字整体隐藏，不会留下空位。
